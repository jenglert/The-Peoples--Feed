class UpdateFeedsController < ApplicationController
  
  # Update all the feeds.
  def index
    #@initialCount = FeedItem.find(:all).length
    
    #@updated_feeds = update_all_feeds()
    
    #@postCount = FeedItem.find(:all).length
  end
  
end